echo "Installing git..."
sudo apt update > /dev/null
sudo apt install -y git > /dev/null

echo "Cloning stable OMAKUB..."
git clone -b stable https://github.com/maicondmenezes-at-celero/omakub.git ~/.local/share/omakub > /dev/null

source ~/.local/share/omakub/install.sh